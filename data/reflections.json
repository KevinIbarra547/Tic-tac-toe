[
  {
    "checkpoint": "CP01-world",
    "title": "Hello World Express scaffold",
    "tag_url": "https://github.com/KevinIbarra547/Tic-tac-toe/releases/tag/CP01-world",
    "what_built": "A minimal Express server scaffold with one route returning \"Hello world\"\nand a static file directory for /public. Just the foundation — no auth,\nno game, no database. Just proof that the project structure works and\nReplit can run it.",
    "what_was_hard": "The hardest part was unwinding what Replit's \"create a new project\"\ntemplate gave me. The starter was a TypeScript pnpm monorepo with Vite\nand a bunch of scaffolding I didn't need. I wiped it and rebuilt as\nplain Node + Express to match the PRD's \"vanilla JavaScript only\"\nconstraint.\n\nThe .replit file had stale [[ports]] entries pointing at Vite's dev\nserver port (5173) and the workflow command was set to pnpm-related\nscripts. I had to manually edit .replit to map localPort=5000 (where my\nExpress server binds) to externalPort=80 (what Replit's preview proxy\nexpects). Without that the preview just showed \"Page not found.\" Took\nsome Googling to figure out the right format.",
    "what_id_do_differently": "Read the PRD's \"allowed packages\" list FIRST before clicking around in\nReplit. I wasted an hour fighting the TypeScript template before I\nrealized the right move was to delete it and start fresh.",
    "ai_usage": "Did the cleanup mostly by hand at first because I didn't yet have Claude\nCode installed locally. Used Replit's built-in AI to help understand the\n.replit port mapping syntax. Once Claude Code was set up on my MacBook,\nthe workflow stabilized for CP02 onwards.",
    "ab_note": "First instinct was to keep the TypeScript starter because it looked\n\"more professional.\" Decided against it because the PRD is explicit:\nvanilla JavaScript only. Ignoring that constraint to feel fancier would\nhave caused problems by CP03 when Claude Code started writing JS that\nthe TypeScript build would have rejected. Sticking close to the PRD's\nconstraints early saved pain later."
  },
  {
    "checkpoint": "CP02-accounts",
    "title": "Signup, login, logout with sessions",
    "tag_url": "https://github.com/KevinIbarra547/Tic-tac-toe/releases/tag/CP02-accounts",
    "what_built": "Signup, login, and logout with express-session middleware. Users are\npersisted as plaintext records in data/users.json (per the PRD's\nlearning-project rule). The frontend has signup/login forms that swap\nout for a logout button when the session is active, and refreshes its\nstatus by hitting GET /me on page load.",
    "what_was_hard": "First time using Claude Code locally — had to install Node.js, npm,\nHomebrew, GitHub CLI, and authenticate everything before I could even\nstart. Hit an EACCES npm permissions issue from running an earlier\nsudo command that I had to fix with chown.",
    "what_id_do_differently": "Read PRs more carefully on the first pass. I almost merged with\nreq.session.user being a string instead of an object — it would\nhave worked for CP02 but broken later when CP05 tries to attach\ngames to a user.",
    "ai_usage": "Claude Code wrote the implementation on a cp02-accounts branch.\nI reviewed the diff against the PRD, caught the req.session.user\nshape inconsistency, and asked Claude Code to fix it before merging.\nDid not blindly approve the first PR.",
    "ab_note": "First version of the prompt I sent to Claude Code was vague — just\n\"build CP02.\" Rewrote it with explicit constraints (allowed packages,\nplaintext passwords, exact route status codes, branch + PR workflow).\nThe second prompt got a clean implementation on the first try."
  },
  {
    "checkpoint": "CP03-game",
    "title": "3x3 grid clickable game board",
    "tag_url": "https://github.com/KevinIbarra547/Tic-tac-toe/releases/tag/CP03-game",
    "what_built": "A 3x3 clickable board with a turn indicator and \"New game\" button. Game\nstate lives as an array of 9 strings in memory, plus a currentTurn variable\nthat flips between X and O. The board is hidden when logged out; logging\nin resets to a fresh empty board. Clicking an empty cell places the mark\nand flips the turn; clicking a marked cell does nothing. No win detection\nyet — that's CP04.",
    "what_was_hard": "Understanding why Claude Code used one click listener on #board instead of\nnine separate listeners on each cell. Had to look up event delegation and\nhow .closest() walks up the DOM to find the actual clickable element.",
    "what_id_do_differently": "Probably write the click handler myself first to make sure I understood\nevent delegation before using it. The pattern is clearly the right call,\nbut it was new to me.",
    "ai_usage": "Claude Code wrote the implementation on a cp03-game branch. I reviewed the\ndiff carefully — verified that no win-detection logic snuck in (that's\nCP04), confirmed the new-game button was inside #game so it'd hide on\nlogout, and traced the disabled-state pattern from JS through CSS.",
    "ab_note": "Originally wanted a click-to-preview-then-confirm pattern. Talked through\nthe tradeoffs — it would double click counts on a 9-click game and add\ncomplexity for the AI in CP06 — and switched to instant click with hover\naffordance. Better choice, simpler code."
  },
  {
    "checkpoint": "CP04-pvp",
    "title": "Win and draw detection",
    "tag_url": "https://github.com/KevinIbarra547/Tic-tac-toe/releases/tag/CP04-pvp",
    "what_built": "Win and draw detection on top of CP03's clickable board. Defined WIN_LINES\nas the 8 winning index triples (3 rows, 3 columns, 2 diagonals). After every\nmove, checkWinner loops through the lines looking for three matching marks.\nA gameOver flag freezes the whole board when someone wins or the board fills\nwithout a winner. The turn indicator text changes to \"X wins!\", \"O wins!\", or\n\"Draw\" depending on outcome.",
    "what_was_hard": "Making sure three empty cells in a row don't register as a win — the\n`board[a] && ...` check at the start of each line comparison handles that.\nSubtle bug I would have shipped if Claude Code hadn't gotten it right the\nfirst time.",
    "what_id_do_differently": "Look up the standard winning-lines pattern earlier — it's the same in basically\nevery tic tac toe tutorial and would have saved me time thinking about it.",
    "ai_usage": "Claude Code wrote the implementation on a cp04-pvp branch. I reviewed the\nmakeMove function carefully — checked that turn flip happens only when game\nis still in progress, not on the winning move. Also checked that the indicator\ntext update was moved out of renderBoard into the state-changing functions,\nwhich is cleaner separation than my CP03 version had.",
    "ab_note": "First version of CP03 had the indicator text inside renderBoard. CP04\nmoved it out into makeMove and newGame so renderBoard is purely about\nsyncing cells from state. Cleaner — one function per concern."
  },
  {
    "checkpoint": "CP05-save",
    "title": "Persist games to JSON, show user history",
    "tag_url": "https://github.com/KevinIbarra547/Tic-tac-toe/releases/tag/CP05-save",
    "what_built": "Server-side persistence for finished games. Added POST /games and GET /games\nroutes to server.js. POST validates the request body (result must be \"win\" or\n\"draw\", board must be a 9-element array), requires login (401 if not), takes\nthe username from the session and the timestamp from the server clock\n(intentionally not from the request body — that would let a logged-in user\nsubmit games as someone else or backdate wins). GET filters to only the\ncurrent user's games and returns newest first.\n\nOn the frontend, added a saveGame() function that POSTs to /games when a\ngame ends in win or draw, then immediately calls loadHistory() to refresh\nthe visible list. Added a \"Your game history\" section that renders past\ngames with formatted timestamps. History clears on logout and reloads on\nlogin.",
    "what_was_hard": "After implementing CP05, games saved to data/games.json correctly but the\nhistory list on the page wouldn't update right after a win — only on a full\npage refresh. Browser console showed 401 Unauthorized errors on /games\nrequests, even though /me worked fine and the user was clearly logged in.\nSpent significant time debugging this:\n\n1. First attempted fix: added credentials: 'same-origin' to all fetch()\ncalls. Helped some but games still saved intermittently and the immediate\nGET still failed sometimes.\n\n2. Second attempted fix (PR #7): switched to credentials: 'include' +\nsameSite: 'none' + secure: true on the server cookie. This is the standard\npattern for cross-origin iframe cookies. Broke login entirely because\nsecure: true requires HTTPS in a way Replit's preview proxy doesn't fully\nsupport. Reverted with git revert -m 1.\n\n3. Tested in incognito (outside the Replit iframe) to isolate code vs\nenvironment. The first game saved but subsequent games still didn't\nappear immediately, which suggested the issue might not just be the\niframe.\n\n4. Reviewed all my files line by line with help to verify the code itself\nwas correct — POST validation, session handling, GET filtering, fetch\noptions, etc. Everything checked out. Deduced the most likely root cause\nis that express-session stores sessions in memory by default, and\nReplit's free tier restarts the workflow process unpredictably, which\nwipes all in-memory sessions. When the server boots back up, my old\nsession ID is in the cookie but the server has no record of it = 401.\n\n5. Considered adding a file-based session store (session-file-store) but\nthat would violate the PRD's allowed-packages rule. Decided to stop\ndebugging on my own and ask my teacher Monday — this seems to be a\nReplit-specific environment issue, not a code issue, and they may have\nexperience with the right setup.\n\nFinal state at end of session:\n- Signup, login, logout work correctly\n- Games save correctly to data/games.json on win or draw\n- Game history displays correctly when the page is loaded or refreshed\n- History does NOT consistently auto-update right after a win (intermittent\n  401s on /games requests, suspected to be sessions being lost when Replit\n  restarts the server process)",
    "what_id_do_differently": "1. Open Chrome DevTools earlier in the debugging — I spent time guessing at\nwhat was broken before actually looking at Network tab status codes.\nOnce I started reading the responses, the diagnosis got way faster.\n2. Test in incognito earlier to separate code bugs from environment\nbugs. That single test would have saved me hours of trying cookie\nconfigs.\n3. Recognize sooner when \"one more fix\" stops being productive. I should\nhave stopped after PR #7 broke login and asked for outside help instead\nof assuming there was a code-level fix I was missing.\n4. Always merge a PR immediately after it's approved, before doing anything\nelse. At one point I had a PR open while testing in Replit, and when the\nnext fix was needed Claude Code branched from a stale main, polluting\nthe diff with already-merged code. Had to close that PR and redo.\n5. Be more skeptical of intermittent bugs. When a bug happens \"sometimes,\"\nthat often means the code is correct and something environmental is\nflaky. Treating intermittent failures the same as deterministic bugs\nwasted time.",
    "ai_usage": "Claude Code wrote all the implementation. Each PR was reviewed before\nmerging — I caught a workflow mistake (PR branched from stale main →\nconfusing diff → close and redo), and verified all the security-sensitive\nparts of the /games routes (auth gating, server-controlled username,\nserver-controlled timestamp, user-scoped GET filter). The code that\nshipped is correct. The bugs we hit were not code bugs.\n\nWhen the cookie issue first appeared, I asked Claude Code to fix it. It\nproduced a clean credentials: 'same-origin' patch that helped partially.\nWhen that wasn't enough, it produced a full iframe-cookie fix that was\ntechnically correct but incompatible with Replit's preview environment.",
    "ab_note": "First diagnosis: \"POST /games is broken.\" Wrong — POST works.\nSecond diagnosis: \"Browser cache problem.\" Wrong — caching wasn't the\nissue.\nThird diagnosis: \"Cookies aren't being sent (same-origin).\" Partially\nright — fixed some symptoms.\nFourth diagnosis: \"Need full cross-origin cookie config (include +\nsameSite: none + secure: true).\" Technically correct but broke login\nin Replit.\nFinal diagnosis: \"Replit's environment restarts the server, wiping\nin-memory sessions. The fix is probably either a different session store\nor some Replit-specific setting.\" Asking teacher for help with this.\n\nLesson: Not all bugs are code bugs. The most expensive part of this checkpoint\nwasn't writing the feature — it was figuring out that what looked like a\ncode bug was actually an infrastructure quirk. Real engineers ship with\nknown limitations and document them. That's also a valid outcome."
  },
  {
    "checkpoint": "CP06-ai",
    "title": "Groq-powered AI opponent",
    "tag_url": "https://github.com/KevinIbarra547/Tic-tac-toe/releases/tag/CP06-ai",
    "what_built": "AI opponent powered by Groq's llama-3.3-70b-versatile model. Added a\nPvP/PvAI mode toggle to the UI. When in PvAI mode and it's the AI's turn,\nthe browser POSTs the current board state to a new /ai-move route. The\nserver builds a structured prompt for Groq (showing the board layout, the\ncurrent state, and the explicit list of legal moves), forces JSON-only\noutput via response_format, parses the move, and validates it's a legal\nempty cell. If Groq returns an invalid move or the API call fails, the\nserver retries up to 3 times, then falls back to picking a random legal\ncell. This guarantees the PRD's \"AI plays legal moves\" requirement holds\neven if the LLM misbehaves.",
    "what_was_hard": "The architecture decision around where to call Groq. If the Groq SDK ran\nin the browser, the API key would be exposed in client-side JavaScript —\nanyone could view source and steal it. The right pattern is to call Groq\non the server, where the API key stays in .env. The browser sends only\nthe board state, gets back only the chosen move. Standard server-side\nsecret pattern.\n\nAlso had to think about UX during AI turns. Without locking the board,\nthe user could click cells while the AI was \"thinking\", causing race\nconditions. Added an aiThinking boolean flag that gates user clicks,\nreleased in a try/finally so it always unlocks even on error.",
    "what_id_do_differently": "Probably show a \"thinking...\" indicator while the AI is computing its\nmove. Right now there's just a 1-2 second pause where nothing visible\nhappens, which could feel like a bug. Could be a small CP07 or CP09\npolish item.",
    "ai_usage": "Claude Code wrote the implementation cleanly in one PR. The /ai-move\nroute had all the security checks I wanted (auth required, body\nvalidation, server-set move validation against available cells), the\nprompt engineering was solid (showed the board visually, listed legal\nmoves explicitly, forced JSON output), and the retry-with-fallback\nlogic guaranteed legal moves even on LLM failure.\n\nI caught one architectural decision in the prompt that mattered: I\nspecified the API call MUST happen server-side, not browser-side. Without\nthat constraint, Claude Code could have built it either way and we'd\nhave had an exposed API key.",
    "ab_note": "Considered letting the player pick X or O. Decided to keep CP06 simple\n(player always X, AI always O, X always first) because the rubric is\n\"AI plays legal moves\" and letter-choice doesn't help that. Saving\n\"player picks letter\" as a possible CP09 custom feature where it would\nactually count toward the 30% custom-features rubric."
  },
  {
    "checkpoint": "CP07-levels",
    "title": "3 difficulties + 3 personalities, hybrid AI",
    "tag_url": "https://github.com/KevinIbarra547/Tic-tac-toe/releases/tag/CP07-levels",
    "what_built": "Three difficulty levels (easy/medium/hard) and three AI personalities\n(pirate/wizard/robot), independently selectable via dropdowns visible\nonly in vs AI mode. The AI now returns BOTH a move AND a short comment\nin character. The comment displays below the board after each AI move\nand clears when the player moves or starts a new game.\n\nArchitecture:\nDifficulty controls how the AI PLAYS (skill level via prompt instructions\n+ temperature tuning: easy=1.2, medium=0.7, hard=0.3). Personality\ncontrols how the AI TALKS (commentary tone via vocabulary instructions).\nThey are completely independent — easy pirate, hard robot, medium wizard,\nall 9 combinations work.\n\nThe prompt is built modularly — base instructions about the board layout,\nplus a difficulty-specific paragraph, plus a personality-specific\nparagraph, plus a structured JSON output requirement. This means adding\na 4th personality later (CP09 maybe?) is just a new entry in two\nconstants, not a rewrite.\n\nRobust fallbacks:\nIf Groq returns invalid JSON, missing move, missing comment, or fails\nentirely, the server falls back per-personality. A pirate AI never\nsuddenly says \"MOVE EXECUTED\" — even on total failure, you get\n\"Arrr, that be me move.\" Took some defensive coding to validate\nmove AND comment independently.",
    "what_was_hard": "The biggest design question was where to put the difficulty/personality\nlogic. Three options I considered:\n1. Hardcoded difficulty (just always medium) and pick personality randomly\n   — too simple\n2. Difficulty as separate algorithm paths (easy = random, hard = minimax)\n   — would work but bypasses Groq for half the modes, which felt like\n   missing the point of having an LLM opponent\n3. Difficulty as prompt engineering — let Groq adjust based on instructions\n   + temperature\n\nWent with option 3. It keeps Groq as the decision-maker for all modes,\nwhich means the experience feels consistent and the personalities can\nsass you about every move. Temperature tuning (1.2 vs 0.3) reinforces\nthe difficulty in a way the prompt alone can't — high temperature makes\neasy mode's \"sometimes pick random\" actually random, while low temperature\nmakes hard mode's \"always block\" actually consistent.\n\n[Update — hard mode fix:]\nAfter tagging CP07-levels initially, I tested and discovered hard mode\nwasn't actually playing well. The LLM consistently failed to block obvious\nthreats — even with explicit \"always block two-in-a-row\" instructions in\nthe prompt. I'd play X in cell 0, then X in cell 1, and the AI wouldn't\nplay cell 2 to block. This made hard mode no harder than easy mode in\npractice.\n\nDiagnosed it as a fundamental LLM limitation: language models don't have\nreliable spatial reasoning. Reading a JSON-encoded board state and\nmentally mapping positions 0-8 to a 3x3 grid is something LLMs do\ninconsistently. Even GPT-4 and Claude make this mistake regularly.\n\nFixed it with a hybrid architecture: deterministic game logic for tactical\nsituations (JS function findTacticalMove that checks all 8 winning lines\nfor \"two-in-a-row with one cell empty\"), LLM for everything else. Hard\nmode now: (1) takes immediate wins, (2) blocks opponent threats, (3) only\nasks Groq for a strategic move when there's no immediate tactical situation.\nPersonality commentary still comes from Groq — even on forced JS moves —\nso the AI still sounds like a pirate/wizard/robot.\n\nBig lesson: LLMs are great at language and pattern-matching. They're bad\nat deterministic logic. Use the right tool for each job. This is actually\nhow real game AI gets built — neural nets for \"intuition\" combined with\nhard-coded rules for situations where the right answer is provable.\n\nEasy and medium modes were left fully Groq-driven on purpose. The LLM's\nimperfection IS the difficulty curve. Trying to force the LLM to play\nworse on easy mode would have been weird; just letting it occasionally\nmiss things naturally produces \"easy\" behavior.\n\nRe-tagged CP07-levels at the post-fix commit so the tag points at the\nworking version.",
    "what_id_do_differently": "The comment displays as static text below the board. A subtle fade-in\nanimation would make it feel more alive. Saving for CP09 polish.\n\nAlso, currently the player has to wait for the comment to appear THEN\nthe AI's mark animates in. The order is: AI calculates → comment\ndisplays → mark appears. Could be smoother. Could also display the\ncomment as a speech bubble next to the board instead of below it.",
    "ai_usage": "Wrote a really long prompt for claude.ai/code that spelled out all 9\ncombinations of difficulty + personality, the validation requirements,\nthe fallback comments, the dropdown UI, and the comment display logic.\nIt got everything right in one PR. Worth noting that I tested this\ncheckpoint on the school iMac using claude.ai/code in browser instead\nof Claude Code in terminal — same agent, different interface, identical\noutput quality.",
    "ab_note": "Considered fancier difficulty implementations (separate code paths for\neach level, minimax algorithm for hard mode). Decided prompt + temperature\nwas cleaner because: (a) it kept Groq as the decision-maker for\nconsistency, (b) made the implementation 30 lines of constants instead\nof 200 lines of game tree code, and (c) the LLM playing imperfectly is\nactually MORE fun than perfect minimax which always draws."
  },
  {
    "checkpoint": "CP08-stats",
    "title": "Leaderboard, AI stats, Checkpoints page",
    "tag_url": "https://github.com/KevinIbarra547/Tic-tac-toe/releases/tag/CP08-stats",
    "what_built": "Three features in one checkpoint: a public leaderboard ranking players\nby win rate (5-game minimum), AI win rate breakdowns by difficulty and\npersonality, and a Checkpoints page that renders all my reflections\nfrom a structured JSON file (data/reflections.json). Also augmented\ngame records to track mode/difficulty/personality so the AI stats\nhave data to aggregate. New routes (/leaderboard, /ai-stats,\n/reflections) are public so the leaderboard works without login —\nfits how leaderboards typically work in classroom settings.\n\nA note on scope:\nThe PRD says CP08 is \"leaderboard + AI win rate by difficulty/personality.\"\nI bundled the Checkpoints page into the same PR even though it's not\nexplicitly part of CP08's spec, because the PRD requires a Checkpoints\npage as a deliverable but doesn't assign it to a specific checkpoint.\nBundling it here ensured the page existed before any more reflections\nwere written. The alternative was making it a CP09 custom feature,\nbut I had stronger ideas for CP09 and CP10 that involve actual gameplay\nfeatures (player picks X/O for CP09, styling pass + game replay for CP10).",
    "what_was_hard": "The data structure decision. Reflections could have been a single\nmarkdown file rendered with a library, or hardcoded HTML, or a\nstructured JSON with named fields. I went with structured JSON because:\n(1) it matches how the rest of the app stores data (games.json,\nusers.json), (2) editing reflections later means just editing a JSON\nfile rather than touching HTML, (3) the rendering code is small and\nreusable across all reflections, (4) adding a new field updates how\nall reflections display.\n\nThe leaderboard logic for PvP win attribution is imperfect — games.json\nrecords \"username\" (the saver) and \"winner\" (X or O) but doesn't track\nwhich user was which letter in PvP. For CP08 I treat the saver as X\nsince the AI is always O in our system, which means PvP wins are\ncorrectly attributed to the saver if they were X. PvP games where the\nsaver was O get incorrectly counted. Documented this as a known\nlimitation; would fix with a proper \"playerLetter\" field on game\nrecords in a future iteration.\n\nOld games from earlier testing don't have mode/difficulty/personality\nfields. AI stats route gracefully skips them via the `if (game.mode\n!== 'pvai' || !game.difficulty || !game.personality) continue;` check.\nOld games still count toward the leaderboard's player win counts.",
    "what_id_do_differently": "Built the Checkpoints page from CP01 onward instead of waiting until\nCP08. The PRD called it out as a deliverable but didn't assign it a\ncheckpoint, so I missed it for the first 7 checkpoints. Catching it\nat CP08 was salvageable but I should have flagged it earlier.",
    "ai_usage": "Wrote a 200+ line prompt for Claude Code with three features specified\nseparately, hard constraints, the data model changes, the public/private\nroute distinction, edge cases for old game data, and the reflection\nfile structure. Got a clean PR back in one shot — 9 file changes,\nall working as specified, no constraint violations. Reviewed the diff\ncarefully; only minor optional polish items I chose not to fix.",
    "ab_note": "Considered hard-coding reflections in HTML before settling on JSON\nstorage. Chat reviewer (the second AI in my AI-checks-AI workflow)\npushed back hard on the HTML approach for maintainability reasons —\nediting HTML for content changes is the exact thing engineers spend\ncareers learning to avoid. Sticking with JSON was clearly correct in\nhindsight; same pattern as games.json in our app already."
  },
  {
    "checkpoint": "CP09-c1",
    "title": "Player picks X or O (custom feature 1)",
    "tag_url": "https://github.com/KevinIbarra547/Tic-tac-toe/releases/tag/CP09-c1",
    "what_built": "A letter selector that lets the player choose to be X or O before each\ngame. The AI plays the other letter. If player picks O, AI plays X and\ngoes first — AI moves automatically when New Game is clicked, no\nextra interaction needed. The CP07 personalities and difficulty levels\nwork the same way regardless of which letter the AI is playing.\n\nThis also retroactively fixed a known limitation in CP08's leaderboard:\ngame records previously didn't track which letter the player chose, so\nPvP win attribution was guessed (assuming saver was always X). Now\nplayerLetter is saved on every game record and the leaderboard route\nuses it for accurate attribution. Old game records (from before this\nchange) fall back to assuming X — backwards compatible.",
    "what_was_hard": "The trickiest part was getting the trigger conditions right. The AI's\nturn fires based on currentTurn matching aiLetter, not based on a\nhardcoded 'O'. The \"clear AI comment\" check fires when player moves,\nnot when X moves. The \"AI moves first\" trigger fires from newGame()\nwhen AI is X, not from the cell click handler. Each of these had been\nhardcoded to 'X' or 'O' in CP07, and missing any of them would have\nbroken the feature subtly (AI never moves, AI moves at wrong time,\ncomments clear at wrong time).\n\nThe Groq prompt also needed parameterization. The original CP07 prompt\nsaid \"You are playing Tic Tac Toe as player O\" — hardcoded. With CP09\nthis becomes dynamic based on aiLetter. Same for the hard-mode tactical\noverride in findTacticalMove — passes aiLetter as the parameter\ninstead of hardcoded 'O'.",
    "what_id_do_differently": "The mini-PRD specified that the letter dropdown is \"visible always.\"\nFor CP09 that's a flat dropdown next to the mode toggle. It works but\nfeels visually busy alongside the difficulty/personality dropdowns\nwhen in PvAI mode. Will redesign in CP10's styling pass — likely\ncombine all settings (mode, letter, difficulty, personality) into a\nsingle options panel or modal.\n\nCould have also added stats tracking by letter (\"X-as-player win rate\nvs O-as-player win rate\") to make the choice feel more measurable.\nDecided that was scope creep for CP09; deferred as a possible future\nfeature.",
    "ai_usage": "Wrote a 200+ line prompt for Claude Code with the architectural\noverview, hard constraints, the data flow changes (frontend +\nbackend), the prompt parameterization, the leaderboard\nbackwards-compat handling, and the mini-PRD content. Got a clean PR\nback in one shot — 5 file changes including the mini-PRD doc, all\nworking as specified.",
    "ab_note": "Initial plan was a separate \"setup screen\" with a Start button before\neach game (Q&A in chat preferred this). Chat reviewer pushed back on\nthe setup-screen approach — too much state machinery for what is just\n\"player can pick a letter,\" and it would have conflicted with how the\nrest of the UI works. Settled on the simpler approach (visible\ndropdown, default X, change resets game). The setup-screen-style\nredesign is now planned as part of CP10's styling pass where it makes\nmore sense.\n\nMini-PRD as a deliverable: documented the feature in cp09-prd.md\nincluding out-of-scope items. The \"what's NOT in this feature\" list\nturned out to be useful for staying focused — it explicitly defers\nanimation, mid-game letter change, stats by letter, and game replay,\nall of which are tempting scope creep."
  },
  {
    "checkpoint": "CP10-c2",
    "title": "Dark redesign + game replay (custom feature 2)",
    "tag_url": "https://github.com/KevinIbarra547/Tic-tac-toe/releases/tag/CP10-c2",
    "what_built": "Two related deliverables in one final checkpoint: (1) a complete visual\nredesign across all three pages (dark theme, electric green accent,\nunified settings panel, motion polish), and (2) a game replay feature\nthat lets the player click any past game in their history and watch\nthe moves play out move-by-move with prev/next/auto-play controls.\n\nThe redesign isn't just a re-skin — it consolidated the four scattered\nselectors from CP07 and CP09 (mode toggle, letter selector, difficulty\ndropdown, personality dropdown) into a single labeled \"Game Settings\"\npanel. CSS classes show/hide difficulty and personality based on mode,\nso the panel looks clean in both PvP and PvAI. Three motion details\n(AI mark fade-in, AI-thinking dots indicator, winning line green glow)\nmake the app feel responsive instead of static.\n\nThe replay feature uses existing data — no schema changes, no new\nserver routes. Game records save the final board, so the replay\nreconstructs the move sequence by interleaving X cells and O cells\nin left-to-right top-to-bottom order. This is positional order, not\nclick order — the final board state matches what actually happened,\nbut the path to get there is approximated. Documented honestly as a\nknown limitation in cp10-prd.md.",
    "what_was_hard": "Two technically interesting problems.\n\nFirst, the replay button event listeners. Each time openReplay() is\ncalled, it needs to wire up Prev/Next/Auto-play to the current game's\ndata. If we just .addEventListener every time, listeners stack up\nacross replay opens — opening the modal three times means each Next\nclick advances three steps. Solution: clone each button via cloneNode,\nreplace the original in the DOM, then attach the listener to the\nclone. This guarantees a clean slate every open. Took me a minute to\nrealize this was happening — symptoms of stale closures often look\nlike \"the feature works the first time then gets weirder.\"\n\nSecond, the AI mark fade-in animation. Removing then immediately\nre-adding a CSS class doesn't restart the animation — the browser\nbatches DOM changes and sees the class state as unchanged. The fix\nis forcing a reflow with `void cell.offsetWidth` between the\nremove and add. This is a documented browser quirk — the offsetWidth\nread forces the layout engine to commit pending changes, which\n\"counts\" the class removal so the re-add triggers a fresh animation.\n\nThe whole replay UI also taught me about modal accessibility. Backdrop\nclicks need to distinguish \"click on backdrop\" from \"click bubbled up\nfrom content\" — solved by checking event.target === modal element.\nAutoplay intervals must be cleared on close OR they keep running and\nmutate state in the closed modal. Buttons need to disable at boundaries\nto prevent currentMoveIndex from going negative.",
    "what_id_do_differently": "Save move sequence on game records going forward (a `moveOrder` array\nsaved when the game ends). The replay reconstruction is fine for old\ngames but doesn't show true click order. With moveOrder saved,\nbuildReplayMoves() could just return moveOrder directly for new games\nand fall back to positional reconstruction for old. About 30 minutes\nof work that I deliberately deferred.\n\nCould have done a light mode toggle. Decided dark-only was a stronger\ndesign statement and easier to keep consistent. Two themes means\ndouble the styling burden and twice the chance of broken contrast.\n\nThe mobile responsiveness is \"doesn't visibly break\" but isn't\nspecifically tuned. The 720px max-width and the 2-column settings grid\nboth adapt reasonably to narrow screens but I didn't add explicit\nbreakpoints.\n\n[Update — true replay follow-up:]\nAfter CP10 shipped, I tested the replay feature and noticed that\ngames where I clicked in non-positional order (center first, then\ncorners) replayed in left-to-right top-to-bottom order instead of\nmy actual click sequence. The replay LOOKED right but didn't show\nwhat actually happened.\n\nFollowed up with a small fix: capture moveOrder as the game is\nplayed (every makeMove call pushes to a moveOrder array), save it\nalongside the final board, and use it in replay when present.\nOld games (saved before this fix) gracefully fall back to the\npositional reconstruction in buildReplayMoves.\n\nServer-side validation was important here — moveOrder is structured\ndata the client could send badly, so each entry gets strict type\nchecks (index 0-8, mark X or O), plus a length sanity check that\nmoveOrder count must match the marks on the final board. Wrong\nclient data → 400, never silently saved.\n\nRe-tagged CP10-c2 at the post-fix commit so the canonical CP10\npoints at the version with true replay.\n\nLesson: \"shipped with documented limitation\" doesn't always mean\n\"don't fix the limitation.\" If the fix is small and addresses the\nactual user experience, ship the fix and update the documentation.\nThe mini-PRD's \"out of scope\" list isn't permanent — it's a snapshot\nof what the FIRST version included.",
    "ai_usage": "Wrote the largest prompt of the project — 300+ lines specifying the\ndesign tokens, every color, the settings panel structure, the replay\nstate machine, the animations, the regression tests, and the mini-PRD.\nGot back a clean PR with all features working in one shot. The fact\nthat it landed cleanly says something about the value of detailed\nspecs: I was essentially handing Claude Code a design document, not\na vague request.\n\nThe chat reviewer (in my AI-checks-AI workflow) caught the move-\nreconstruction limitation during diff review and suggested I either\nship it as-is, augment with moveOrder, or remove auto-play. Decided\nship-as-is + document was the right call for a project at the\nfinish line.",
    "ab_note": "Original CP10 plan was a separate setup screen for picking letter\n(deferred from CP09) — chat reviewer pushed back on that as\noverengineering. Settled on the unified settings panel which solves\nthe same UX problem (all options grouped) without the state machine\ncomplexity of a separate screen. Cleaner architecture, easier to\nimplement, looks more polished.\n\nConsidered themed coloring per AI personality (pirate = red, wizard =\npurple, robot = cyan). Decided single accent (#4ade80) was better —\nvisual coherence across personalities, no color palette explosion,\neasier to review the diff. Personality is communicated through text\n(commentary) which is the right channel.\n\nConsidered light mode. Decided dark-only was a stronger design\nstatement and easier to keep consistent."
  }
]